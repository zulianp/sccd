# Figures

Generated, not tracked. `scripts/plot.sh` writes here from the cross-check tables
in `research/csv/`, which `scripts/crosscheck.sh` produces. Only this file is
committed; everything else in this directory is output and is ignored.
